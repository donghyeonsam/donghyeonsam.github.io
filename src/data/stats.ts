// Captions for the masthead's left/right stat boxes. The numbers themselves
// are live (see useGithubCommitCount + til/techlogs list lengths in
// MagazinePage) rather than hardcoded here.
export const statCaptions = {
  commits: 'Total Commits Made',
  tilTechlogs: 'TIL & Tech Logs',
}
